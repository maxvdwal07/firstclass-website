import { defineConfig } from "astro/config";

export default defineConfig({
  // Verander dit naar je GitHub gebruikersnaam en repository naam
  // Voorbeeld: site: "https://jouwgebruikersnaam.github.io"
  //            base: "/kapper-website"
  site: "https://maxvdwal07.github.io",
  base: "/larsa-website",
  devToolbar: { enabled: false },
});
